name: publish aur

on:
  workflow_call:
    inputs:
      plan:
        required: true
        type: string

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      PLAN: ${{ inputs.plan }}

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ fromJson(inputs.plan).announcement_tag }}

      - name: get version
        id: version
        shell: bash
        run: |
          version="$(jq --raw-output '.releases[0].app_version' <<< "$PLAN")"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: prepare rmcl
        run: |
          sed -i "s|^pkgver=.*|pkgver=${{ steps.version.outputs.version }}|" pkg/rmcl/PKGBUILD
          cp pkg/rmcl/PKGBUILD PKGBUILD

      - name: update checksums for rmcl
        uses: datakrama/archlinux-package-action@v2
        with:
          updpkgsums: true
          srcinfo: true
          flags: ""
          namcap: false

      - name: publish rmcl to AUR
        uses: KSXGitHub/github-actions-deploy-aur@v4.1.3
        with:
          pkgname: rmcl
          pkgbuild: PKGBUILD
          assets: .SRCINFO
          commit_username: objz
          commit_email: me@objz.dev
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          commit_message: "Update to ${{ steps.version.outputs.version }}"

      - name: prepare rmcl-bin
        run: |
          sed -i "s|^pkgver=.*|pkgver=${{ steps.version.outputs.version }}|" pkg/rmcl-bin/PKGBUILD
          sed -i "s|^pkgrel=.*|pkgrel=1|" pkg/rmcl-bin/PKGBUILD
          cp pkg/rmcl-bin/PKGBUILD PKGBUILD
          rm -f .SRCINFO

      - name: update checksums for rmcl-bin
        uses: datakrama/archlinux-package-action@v2
        with:
          updpkgsums: true
          srcinfo: true
          flags: ""
          namcap: false

      - name: publish rmcl-bin to AUR
        uses: KSXGitHub/github-actions-deploy-aur@v4.1.3
        with:
          pkgname: rmcl-bin
          pkgbuild: PKGBUILD
          assets: .SRCINFO
          commit_username: objz
          commit_email: me@objz.dev
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          commit_message: "Update to ${{ steps.version.outputs.version }}"

      - name: prepare rmcl-git
        run: |
          cp pkg/rmcl-git/PKGBUILD PKGBUILD
          rm -f .SRCINFO

      - name: update metadata for rmcl-git
        uses: datakrama/archlinux-package-action@v2
        with:
          updpkgsums: true
          srcinfo: true
          flags: ""
          namcap: false

      - name: publish rmcl-git to AUR
        uses: KSXGitHub/github-actions-deploy-aur@v4.1.3
        with:
          pkgname: rmcl-git
          pkgbuild: PKGBUILD
          assets: .SRCINFO
          commit_username: objz
          commit_email: me@objz.dev
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          commit_message: "Update package metadata"
